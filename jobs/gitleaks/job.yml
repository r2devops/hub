name: gitleaks
description: A secret detection job, using gitleaks to alert on secrets (passwords, api keys…) pushed in your code
default_stage: static_tests
icon: 🔐
maintainer: FulcrandG
license: MIT
images:
  "zricethezav/gitleaks": "v8.6.0"
tools:
  "gitleaks": "8.6.0"

labels:
  - GitLab
  - Security
  - SAST
